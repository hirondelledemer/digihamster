import React, { ReactNode } from "react";

export const DryGarden = ({ children }: { children: ReactNode }) => (
  <g
    transform="matrix(1.316963, 0, 0, 1.37333, -220.464249, -156.295731)"
    style={{}}
  >
    <title>{"DryGarden"}</title>
    {children}
  </g>
);
